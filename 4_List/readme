## How to run
- `make`: Compile the code.
- `make report`: Run the LaTeX code to generate the report.
- `make clean`: Clean the executable file.

## Bugs I have found
There are two bugs in the `testbug()` function in `List.cpp`.

```cpp
    // testbug();  // Uncomment this line 123 to test the bug
```